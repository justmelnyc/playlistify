import React from 'react'
// import Playlist from './../../client/containers/Playlist'
import { mount } from 'enzyme'

describe('Playlist Component', () => {
  it('sanity check', () => {
    // const wrapper = mount(<Playlist />)
    // expect(wrapper).not.toBeNull()
  })
})
